# ── Supabase (replaces MongoDB + Cloudinary + NextAuth) ─────────────────────
# Found in: Supabase Dashboard → Project Settings → API

NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ── Supabase SQL schema (run once in Supabase SQL Editor) ────────────────────
#
# create table events (
#   id uuid primary key default gen_random_uuid(),
#   title text not null,
#   date text not null,
#   time text not null,
#   type text check (type in ('liturgy','study','feast')) not null,
#   desc text default '',
#   created_at timestamptz default now()
# );
#
# create table gallery_photos (
#   id uuid primary key default gen_random_uuid(),
#   storage_path text not null,
#   public_url text not null,
#   caption text default '',
#   section text check (section in ('gallery','about','home')) default 'gallery',
#   created_at timestamptz default now()
# );
#
# -- Allow public read on both tables:
# alter table events enable row level security;
# create policy "Public read" on events for select using (true);
# create policy "Auth write" on events for all using (auth.role() = 'authenticated');
#
# alter table gallery_photos enable row level security;
# create policy "Public read" on gallery_photos for select using (true);
# create policy "Auth write" on gallery_photos for all using (auth.role() = 'authenticated');
#
# -- Storage bucket (in Supabase Dashboard → Storage → New bucket):
# Name: church-images
# Public: yes
